# In this case you need obtain a list of divisors of a given number
#For example, if you give the number 6 to the function, the result should be [1,2,3,6]

def allDivisors(number):
	li = []
	for i in range(1, number+1):
		if number % i == 0:
			li.append(i)
	print(li)
allDivisors(int(input("Please enter a number: ")))

